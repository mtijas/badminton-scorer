{
  "name": "@badminton-scorer/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @badminton-scorer/shared build && vite --host 0.0.0.0",
    "build": "pnpm --filter @badminton-scorer/shared build && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@badminton-scorer/shared": "workspace:*",
    "@vitejs/plugin-react": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
