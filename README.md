# Badminton Scorer

Starter structure for a React and Node.js badminton scoring service.

The initial product scope is players, matches, point recording, game/match winner calculation, and live match state.

Implementation commands and setup instructions will be added when the stack is selected.

ESLint is the repository-wide linter. Once dependencies are installed, run `npm run lint` from the repository root.
